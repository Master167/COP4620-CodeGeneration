int sub(int x, int y)
{
	int m[10];
	int p;
	p = m[p] + p * x;
	return p;
}

void main(void) {
	int x;
	int w;
	int v[15];
	float q;
	while ( x * w - w >= w + v[3])
	{ w = w + x + w * v[4]; }
	x = sub(x, q);
}